<UserControl x:Class="RedBuilt.Revit.BundleBuilder.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedBuilt.Revit.BundleBuilder.Views"
             mc:Ignorable="d" 
             Height="450" 
             Width="800">
    <Grid Margin="10">

        <!-- Main Content -->
        <StackPanel Margin="0,50,0,70">
            
        </StackPanel>

        <!-- Header -->
        <Label Content="Customize Settings" Style="{StaticResource title-left}"/>

        <!-- Buttons -->
        <Button Content="Save" Style="{StaticResource button-right}" Command="{Binding SaveAndNavigateCommand}"/>
        <Button Content="Cancel" Style="{StaticResource button-left}" Command="{Binding NavigateBundleViewCommand}"/>
        
    </Grid>
</UserControl>
