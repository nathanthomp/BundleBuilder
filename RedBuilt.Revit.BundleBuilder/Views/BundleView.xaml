<UserControl x:Class="RedBuilt.Revit.BundleBuilder.Views.BundleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RedBuilt.Revit.BundleBuilder.Views"
        mc:Ignorable="d"
        Height="450" 
        Width="800">
    <Grid Margin="10">
        
        <!-- Main Content -->
        <ListView x:Name="panels" Margin="0,50,0,70">
        </ListView>

        <!-- Header -->
        <Label Content="Select Panels To Bundle" Style="{StaticResource title-left}"/>

        <!-- Buttons -->
        <Button Content="Bundle" Style="{StaticResource button-right}" Command="{Binding BundleAndNavigateCommand}"/>
        <Button Content="Settings" Style="{StaticResource button-left}" Command="{Binding NavigateSettingsCommand}"/>

    </Grid>
</UserControl>
