<UserControl x:Class="RedBuilt.Revit.BundleBuilder.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedBuilt.Revit.BundleBuilder.Views"
             mc:Ignorable="d" 
             Height="450"
             Width="800">
    <Grid Margin="10">
        
        <!-- Main Content -->
        
        <!-- Header -->
        <Label Content="{Binding Header}" Style="{StaticResource title-left}"/>
        <Label Content="Export Bundles" Style="{StaticResource title-right}"/>

        <!-- Buttons -->
        <Button Content="Export" Style="{StaticResource button-right}" Command="{Binding ExportCommand}"/>
        <Button Content="Cancel" Style="{StaticResource button-left}" Command="{Binding RestartAndNavigateCommand}"/>

    </Grid>
</UserControl>
